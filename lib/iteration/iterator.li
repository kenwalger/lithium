import Interface from './interface.li'

export default class Iterator extends Interface

	# To implement Iterator, an object must have a 'next' method.

	static [Symbol.isA](lhs)
		if ('next' in lhs and lhs.next is a Function)
			return true
		else
			return false
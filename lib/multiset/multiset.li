export default class Multiset extends Bag

	[Symbol.constructor](data, options := {})



	[Symbol.call](Class type)

		return class extends Multiset 

			[Symbol.elementType] := type


	contains(element)

	count(element)

	union(Multiset multiset)

		return new Multiset([...this, ...multiset])
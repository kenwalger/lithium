import Symbol from 'symbol'

export default class Rope extends String

	[Symbol.constructor](iterable, options)
  
	index(Integer i)
  
 	concatenate(Rope s)
  
	split(Integer i)

	insert(Integer i, Rope s))
#!/usr/bin/env ruby

require 'lithium'

rawArgs = ARGV
parsedArgs = {}
whatToDo = ''

case rawArgs[0]
when 'install'
when 'run'

when 'compile'
else
	if
		whatToDo = 'run'
		parsedArgs['file'] = 
	else
	end
end

while rawArgs.length > 0
	if 
		if condition
			
		else
		end
	end
end

lithium[whatToDo](parsedArgs)